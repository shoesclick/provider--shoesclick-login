apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: provider--shoesclick-login-hpa
  namespace: shoes-eks
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: provider--shoesclick-login-deployment
  minReplicas: 1
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50
  